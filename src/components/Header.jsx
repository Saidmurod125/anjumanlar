import Navbar from "./Navbar";

const Header = () => {
  return (
    <header className="w-full min-h-screen bg-gray-100 dark:bg-gray-900">
      
     <Navbar/>
    </header>
  );
};

export default Header;
